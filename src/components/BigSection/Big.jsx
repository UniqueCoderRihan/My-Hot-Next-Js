import useMe from '@/Hooks/useMe';
import React from 'react';

const Big = () => {
    const heloo = useMe
    return (
        <div>
            hello iam from Big Section
        </div>
    );
};

export default Big;